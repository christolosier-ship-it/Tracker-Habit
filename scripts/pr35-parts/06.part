te_text(source)

for obsolete in [
    "src/features/mascot/DashboardMascot.test.tsx",
    "src/features/mascot/mascot-config.ts",
    "src/features/mascot/mascot-config.test.ts",
]:
    path = ROOT / obsolete
    if path.exists(): path.unlink()

for part in (ROOT / "scripts/pr35-parts").glob("*.part"):
    part.unlink()
(ROOT / "scripts/pr35-parts").rmdir()
Path(__file__).unlink()
